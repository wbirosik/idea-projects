object HelloWorld {
  def main(args: Array[String]): Unit = {
    print("pal")

    // Variable (mutable)
      var some_var = "some var"
      var some_var2 : String = "some var"
    // Variable (immutable)
    val some_val  = "some var"
    val some_val2 : Int = 89

    val pallens = "name"
  }}

